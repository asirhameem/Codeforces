#include <stdio.h>
#include <stdlib.h>
int main()
{
    int i;
    char word[50];
    printf("Enter the word:");
    scanf("%s", word);
    for (i=0;i<555;i++)
        printf("%s\t", word);
    return 0;
}
