#include <iostream>

using namespace std;

int main()
{
    int n;
    cin >> n;
    int p , q;
    cin >> p ;

    int pArr[n];
    int count = 0;
    for(int m = 0; m < p; m++)
    {
        int done;
        cin >> done;
        if(m == 0)
        {
            pArr[count] = done;
        }
        for()

    }
    int q;
    cin >> q;
    for(int j = 0; j < q; j++)
    {
        int val;
        cin >> val;

    }

}
